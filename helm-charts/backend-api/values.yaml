replicaCount: 2
image:
  repository: <YOUR_DOCKER_REGISTRY>/backend-api
  tag: "{{ .Values.image.tag }}"
  pullPolicy: IfNotPresent
service:
  type: ClusterIP
  port: 80
ingress:
  enabled: true
  hosts:
    - host: backend-api.<DOMAIN>
      paths:
        - path: /
          pathType: Prefix
resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
